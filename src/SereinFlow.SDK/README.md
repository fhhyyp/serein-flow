# SereinFlow .NET Client SDK

`SereinFlow.SDK` provides a typed HTTP client for invoking SereinFlow flows,
tracking runs, publishing messages, reading events, and downloading workpieces.

The public namespace is `SereinFlow.Client`.
