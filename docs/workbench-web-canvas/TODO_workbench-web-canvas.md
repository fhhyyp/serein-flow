# Web 工作台多画布与国际化后续事项

> 本文件只记录本切片后续工作，不影响已验收的本地编辑能力。

## 必须在 T8/T10 后续切片完成

- [ ] 将 `CanvasState` 与 REST/OpenAPI DTO 双向映射，保存 `ConnectionKind.Execution`、`ConnectionKind.Data`、参数来源和并发版本；处理加载、保存、冲突与校验错误。
- [ ] 用服务端节点定义替换示例节点，覆盖多输出、复杂参数、动态端口和生命周期画布。
- [ ] 接入 Vue Router、Pinia 和 Vue Query，区分工作区局部编辑状态与服务端缓存状态。
- [x] 实现本地工作副本的撤销/重做、未保存变更提示、保存恢复和 Ctrl/Cmd 快捷键；详细记录见 `../workbench-editing-history/`。
- [ ] 实现复制/粘贴、对齐/框选和可访问的命令入口。
- [ ] 接入流程校验、保存、运行/取消，以及 SignalR 优先、SSE 降级的运行状态与事件流。
- [ ] 集成 Monaco 与 ScriptLang LSP，但浏览器仍不得加载脚本引擎或插件程序集。

## 质量门禁

- [ ] 建立 Playwright E2E：画布隔离、双语义连线、参数来源回退、删除、保存冲突和语言持久化。
- [ ] 建立 axe/WCAG 2.2 AA、键盘焦点和中英文文本溢出测试。
- [ ] 覆盖 375/768/1024/1440px 的视觉回归；为小屏设计完整的节点库和检查器抽屉操作。
- [ ] 对大图（1000 节点/1500 边）建立首次可交互、平移缩放和内存性能基线。
