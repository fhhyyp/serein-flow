# Worker Protocol v2 (Compatibility Path)

Worker Protocol v1 and v2 are now consolidated in the [Worker Protocol](worker-protocol.md).

This path is retained for old links. The current production v2 protocol, message
bridge, stable error codes, and test boundary are maintained in the unified document.
