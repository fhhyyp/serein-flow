# Worker Protocol v1 (Historical Path)

Worker Protocol v1 and v2 are now consolidated in the [Worker Protocol](worker-protocol.md).

This path is retained for old links. The v1 version, base messages, stable error
codes, and historical verification scope are maintained in the unified document's
historical compatibility section.
