# TODO: Blank Project Default

## Deferred work

1. Add browser E2E coverage for a first-run database that opens the real Vue editor, verifies the visible canvas is empty, and saves it through the Vite proxy.
2. Add an API integration test fixture with a disposable SQLite database. The current delivery validates the route with a real temporary HTTP instance, while the repository's API integration project currently has no discovered tests.
3. Define an explicit user-selectable entry-node interaction before enabling a production Run command. The current mapper retains the existing rule that the first `Main` canvas node is the entry.

## No action required

No configuration, credential, migration, legacy `.dnf` conversion, or seed-data cleanup is required for this change.

